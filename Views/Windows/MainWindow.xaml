<Window x:Class="KYSQLhelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:KYSQLhelper.ViewModels"
        xmlns:fa="http://schemas.fontawesome.com/icons/"
        xmlns:local="clr-namespace:KYSQLhelper"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="680" Width="800" WindowStartupLocation="CenterScreen" x:Name="App"
        Icon="/Resourse/Ico/sqlicon.ico"
        >
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <StackPanel>
        
        <!--#region Connection -->
        <TextBlock Text="Connection credentials" FontWeight="Bold" Margin="10 5"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="15*"/>
                <ColumnDefinition Width="34*"/>
                <ColumnDefinition Width="49*"/>
                <ColumnDefinition Width="49*"/>
                <ColumnDefinition Width="49*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Label Grid.Column="0" Grid.Row="0" Content="Ip adress" Margin="8 0 10 0" Grid.ColumnSpan="2" />
            <Label Grid.Column="2" Grid.Row="0" Content="User" Margin="8 0 10 0" />
            <Label Grid.Column="3" Grid.Row="0" Content="Password" Margin="8 0 10 0" />

            <TextBox Grid.Column="0" Grid.Row="1" Margin="10 0 10 0" Padding="2" Grid.ColumnSpan="2" Text="{Binding IpAdress}" />
            <TextBox Grid.Column="2" Grid.Row="1" Margin="10 0 10 0" Padding="2" x:Name="UserName" Text="{Binding UserName}"/>
            <TextBox Grid.Column="3" Grid.Row="1" Margin="10 0 10 0" Padding="2" x:Name="Password" Text="{Binding Password}"/>

            <!--Button Style="{StaticResource RoundButtonTemplate}" FontWeight="Bold"  Grid.Column="4" Grid.Row="1" Content="Connect" Margin="10 0 10 0" x:Name="SqlConnectBtn" Click="SqlConnectBtn_Click" Command="{Binding SqlConnectCommand}"/-->
            <Button Style="{StaticResource ConnectButtonTemplate}" FontWeight="Bold"  Grid.Column="4" Grid.Row="1" Content="Connect" Margin="10 0 10 0" x:Name="SqlConnectBtn" Command="{Binding SqlConnectCommand}"/>
        </Grid>

        
        <!--#endregion-->
        <!--#region Query -->
        <TextBlock Text="Search Option" FontWeight="Bold" Margin="10 10"/>
        <Grid>
            <!--#region Col defenition -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!--#endregion-->
            <!--#region Row defenition -->
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!--#endregion-->
            <!--#region FROM -->
            <Label Grid.Column="0" Grid.Row="0" Content="From DB" Margin="10 0"/>
            <ComboBox Grid.Column="0" Grid.Row="1" Margin="10 0 10 0" x:Name="ComboBoxDB" SelectionChanged="ComboBoxDB_SelectionChanged"/>

            <Label Grid.Column="0" Grid.Row="2" Content="From Table" Margin="10 0"/>
            <ComboBox Grid.Column="0" Grid.Row="3" Margin="10 0 10 0" x:Name="ComboBoxTable" SelectionChanged="ComboBoxTable_SelectionChanged"/>
            <!--#endregion-->
            <!--#region SELECT -->
            <Label Grid.Column="1" Grid.Row="0" Content="Select" Margin="10 0"/>
            

            <Grid Grid.Column="1" Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0"  Content="AND" Margin="10 0"/>
                <!--" CheckBox Grid.Column="1" x:Name="SelectSecondItem" Content="Use" VerticalAlignment="Center"  /-->
            </Grid>
            
            

            <Grid Grid.Column="1" Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0"  Content="AND" Margin="10 0"/>
                <!--" CheckBox Grid.Column="1" x:Name="SelectThirdItem" Content="Use" VerticalAlignment="Center"/-->
            </Grid>

            <ComboBox Grid.Column="1" Grid.Row="1" Margin="10 0 10 0" x:Name="ComboBoxSelectPrime">
                <ComboBoxItem Content="All"/>
            </ComboBox>
            <ComboBox Grid.Column="1" Grid.Row="3" Margin="10 0 10 0" x:Name="ComboBoxSelectSecond"></ComboBox>
            <ComboBox Grid.Column="1" Grid.Row="5" Margin="10 0 10 0" x:Name="ComboBoxSelectThird"></ComboBox>
            <!--#endregion-->
            <!--#region WHERE -->


            <Grid Grid.Column="2" Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Column="0"  Content="Where" Margin="10 0"/>
            </Grid>

            <ComboBox Grid.Column="2" Grid.Row="1" Margin="10 0 10 0" x:Name="ComboBoxSelectWhere">
            </ComboBox>
            <!--#endregion-->

            <!--Compare type-->
            <!--#region Compare type -->
            <Label Grid.Column="3" Grid.Row="0" Content="Compare type" Margin="10 0"/>
            <Label Grid.Column="3" Grid.Row="2" Content="Input" Margin="10 0"/>

            <TextBox Grid.Column="3" Grid.Row="3" Padding="2" x:Name="CompareInput" Margin="10 0 10 0"/>

            <ComboBox Grid.Column="3" Grid.Row="1" Margin="10 0 10 0" x:Name="ComboBoxCompareType">
                <ComboBoxItem Content="LIKE"/>
                <ComboBoxItem Content="BETWEEN"/>
                <ComboBoxItem Content="EQUAL"/>
            </ComboBox>
            <!--#endregion-->

            <!--Sort by-->
            <Label Grid.Column="4" Grid.Row="0" Content="Sort By" Margin="10 0"/>
            <ComboBox Grid.Column="4" Grid.Row="1" Margin="10 0 10 0" x:Name="ComboBoxSort">
            </ComboBox>

            <!--Order by-->
            <Label Grid.Column="5" Grid.Row="0" Content="Order By" Margin="10 0"/>
            <ComboBox Grid.Column="5" Grid.Row="1" Margin="10 0 10 0" x:Name="ComboBoxOrder">
                <ComboBoxItem Content="ASC"/>
                <ComboBoxItem Content="DESC"/>
            </ComboBox>

        </Grid>
        <!--#endregion-->
        <!--#region Output table -->
        <TextBlock Text="Output" FontWeight="Bold" Margin="10 10"/>

        <DataGrid x:Name="dataGrid" CanUserAddRows="False" MinHeight="150" MaxHeight="200" AutoGenerateColumns="True" Margin="10 10 10 10"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Content="Execute Query" Style="{StaticResource RoundButtonTemplate}" x:Name="ExecuteQuery" Click="ExecuteQuery_Click" Grid.Column="1" Margin="0 10 10 0" Background="red" FontWeight="Bold"/>
            <Button Content="Clear Table" Style="{StaticResource RoundButtonTemplate}" Grid.Column="2" Margin="0 10 10 0" x:Name="ClearTable" Click="ClearTable_Click" />
        </Grid>
        <!--#endregion-->
        <!--#region Log -->
        <TextBlock Text="Log View" FontWeight="Bold" Margin="10 10"/>

        <TextBox x:Name="LogBox" Height="50" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" Margin="10 10 10 10"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Content="Clear Log" Style="{StaticResource RoundButtonTemplate}" Grid.Column="2" Margin="0 10 10 0" />
        </Grid>
        <!--#endregion-->

    </StackPanel>

</Window>
